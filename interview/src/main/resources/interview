Из яндекса:

Вставка в середину ArrayList и LinkedList:
При вставке в середину ArrayList если размера не хвататет требуется увеличение памяти и копирование массива и сдвиг.
При вставке в середину LinkedList дойти до нужной ноды и вставить новую ноду.
Иерархия коллекций:
Iterable->Collection->List
                    ->Queue
                    ->Set
        ->Map

Отличие TreeMap от HashMap:
TreeMap хранит ключи в упорядоченном состоятнии.

Отличие HashSet от TreeSet:
TreeSet хранит значения в упорядоченном сотоянии.